# Put your contract documents here

Drop your assigned **Legal Contracts** PDFs (or `.txt` / `.md` files) in this
folder, then upload them through the app's sidebar.

You can also ingest them straight from the command line:

    python ingest_cli.py docs/
